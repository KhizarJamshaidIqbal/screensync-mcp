{
  "_about": "What the destructive-action appraisal must and must not flag. Read by extension/test/destructive_vocab.test.js and mcp-server/test/destructive_vocab.test.ts so the hub, the extension and the page-side units are held to one table.",
  "code": {
    "safe": [
      "JSON.stringify((() => { const dd = document.querySelector('[data-nav-dropdown]'); const panel = dd.querySelector('div'); return {...}; })())",
      "JSON.stringify((() => { const dd = document.querySelector('[data-nav-dropdown]'); const panel = dd.querySelector('div'); return { hasDropdown: !!dd, open: panel && getComputedStyle(panel).display }; })())",
      "document.querySelectorAll('.dropdown-menu .dropdown-item').length",
      "[...document.querySelectorAll('nav li')].map((li) => li.classList.contains('dropdown'))",
      "getComputedStyle(document.querySelector('.modal-backdrop')).opacity",
      "getComputedStyle(document.querySelector('.card')).filter.includes('drop-shadow')",
      "document.querySelector('.card').style.filter === 'drop-shadow(0 0 2px #000)'",
      "[...document.querySelectorAll('li')].filter((li) => li.dataset.dropped === 'true').length",
      "items.filter((item) => !item.deleted).length",
      "document.querySelector('.undeletable')?.textContent",
      "window.removeEventListener('scroll', onScroll)",
      "el.removeEventListener('click', handler); return true",
      "document.querySelector('.price-display').textContent",
      "JSON.parse(document.querySelector('#payload').textContent)",
      "document.querySelector('a.buyer-guide').href",
      "location.href",
      "window.location.pathname === '/tours'",
      "document.cookie.split(';').length",
      "localStorage.getItem('theme')",
      "document.title",
      "document.querySelector('.nav').classList.contains('dropDown') && toggleDropDown",
      "[...document.querySelectorAll('.card')].filter((c) => c.dataset.removed !== 'true').map((c) => c.dataset.displayName)",
      "document.querySelector('#update-banner').textContent"
    ],
    "destructive": [
      "db.exec('DROP TABLE users')",
      "run(`DELETE FROM bookings WHERE id = 1`)",
      "document.querySelector('#row').remove()",
      "document.querySelector('form#checkout').submit()",
      "document.forms[0].requestSubmit()",
      "fetch('/api/items/1', { method: 'DELETE' })",
      "fetch('/api/items', { method: 'POST', body: '{}' })",
      "fetch(url, {method:\"PUT\", body})",
      "fetch(url, { method: 'PATCH' })",
      "localStorage.clear()",
      "sessionStorage.removeItem('cart')",
      "document.cookie = 'session=; expires=Thu, 01 Jan 1970 00:00:00 GMT'",
      "location.href = 'https://example.com/logout'",
      "window.location.assign('/logout')",
      "location.replace('/signout')",
      "document.querySelector('button.primary').click()",
      "window.open('https://example.com/checkout')",
      "indexedDB.deleteDatabase('app')",
      "document.querySelector('.delete-account').textContent",
      "document.querySelector('#buyNowButton')",
      "cancelSubscription()",
      "document.querySelector('#deleteaccount').textContent",
      "api('/v1/removeuser')",
      "document.querySelector('.autopay-toggle')"
    ]
  },
  "ui": {
    "safe": [
      "Dropdown menu",
      "#main-nav .dropdown-toggle",
      "Display settings",
      "Show deleted items",
      "div.modal-backdrop",
      "Dropped pins",
      "Save draft",
      "#save-draft",
      "Update profile",
      "Report a problem",
      "Buyer guide"
    ],
    "destructive": [
      "Delete account",
      "button.delete-account",
      "Remove item",
      "Buy now",
      "#buyNowButton",
      "[data-action=deletePost]",
      "Pay $20",
      "Proceed to payment",
      "Cancel subscription",
      "Purchase",
      "DROP TABLE",
      "Enable autopay",
      "Repay loan",
      "Recharge now",
      "#deleteaccount",
      "CANCELSUBSCRIPTION",
      "deletedforever",
      "#removedaccount",
      "[data-action=chargedcardnow]",
      "droppeddatabase",
      "dropdowndatabase",
      "removeEventListenerAndDeleteAccount",
      "#removeEventListenerAccount"
    ]
  }
}
